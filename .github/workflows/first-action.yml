name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings
        run: echo "Hello Ritwik!"
      - name: Print good bye
        run: echo " Good Byee!"
